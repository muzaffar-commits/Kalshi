/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  basePath: "/demo/2",
  assetPrefix: "/demo/2",
  images: {
    unoptimized: true,
  },
};

export default nextConfig;


